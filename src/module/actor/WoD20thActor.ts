import Data = Actor.Data;

export class WoD20thActor extends Actor {
    /*constructor(data:Data, options) {
        super(data, options);
    }*/

    prepareData() {
        super.prepareData();
    }
}