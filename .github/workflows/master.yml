name: WoD Deploy CI
on:
  push:
    branches:
      - main
      - wod-1.0.0-dev
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
      - uses: actions/checkout@v1.0.0
      - name: Install and Build 🔧
        run: |
          npm install
          gulp build
      - name: Switch to the dist folder
        run: |
          cd dist
          ls -la
      - name: ZIP Project
        run: zip -r wod-foundry-vtt.zip .
      - name: Released to Github
        run: echo "Done!"